import { ResourceWorkspace } from '@/components/layout/ResourceWorkspace'
type ModulePageProps = { resourceType?: string; eyebrow: string; title: string; description: string; primaryAction?: string; metrics?: readonly [string, string, string][]; columns?: readonly string[]; emptyMessage?: string }

export function ModulePage({ resourceType, eyebrow, title, description, primaryAction = 'Create', metrics = [['Total records', '0', 'No records yet'], ['Needs attention', '0', 'No open exceptions'], ['Updated today', '—', 'Waiting for activity']], columns = ['Name', 'Status', 'Updated', 'Actions'], emptyMessage = 'No records have been created in this workspace yet.' }: ModulePageProps) {
  if (resourceType) return <ResourceWorkspace resourceType={resourceType} eyebrow={eyebrow} title={title} description={description} primaryAction={primaryAction} columns={columns} />
  return <section><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-2 text-xs font-bold uppercase tracking-[0.2em] text-forest">{eyebrow}</p><h1 className="text-3xl font-bold tracking-tight">{title}</h1><p className="mt-2 text-slate-500">{description}</p></div><button className="rounded-xl bg-forest px-4 py-3 text-sm font-bold text-white">+ {primaryAction}</button></div><div className="mb-6 grid gap-4 md:grid-cols-3">{metrics.map(([label, value, note]) => <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel" key={label}><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-3xl font-bold">{value}</p><p className="mt-2 text-xs text-slate-400">{note}</p></article>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-panel"><div className="mb-5 flex items-center justify-between"><div><h2 className="text-lg font-bold">{title} registry</h2><p className="mt-1 text-sm text-slate-400">Filter, review, and manage {title.toLowerCase()}.</p></div><button className="rounded-xl border border-slate-200 px-3 py-2 text-sm text-slate-600">Export</button></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr>{columns.map((column) => <th className="px-3 py-3 first:pl-0" key={column}>{column}</th>)}</tr></thead><tbody><tr><td className="py-14 text-center text-slate-400" colSpan={columns.length}>{emptyMessage}</td></tr></tbody></table></div></div></section>
}
