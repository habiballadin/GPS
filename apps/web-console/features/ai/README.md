# AI feature

Own assistant conversations, typed tool results, approval dialogs, citations, evaluations, and AI action audit views here. The UI must never authorize mutations; the backend remains the policy boundary.
