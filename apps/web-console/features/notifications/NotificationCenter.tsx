'use client'
import { useEffect, useState } from 'react'
import { useAuth } from '@/components/providers/AuthProvider'
export function NotificationCenter() { const { token } = useAuth(); const [email, setEmail] = useState(true); const [sms, setSms] = useState(false); const [message, setMessage] = useState(''); const save = async () => { const response = await fetch('/api/v1/notifications/preferences', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ name: 'default', status: 'active', description: 'Notification preferences', details: { email, sms, push: false } }) }); setMessage(response.ok ? 'Preferences saved.' : 'Could not save preferences') }; useEffect(() => { if (token) void fetch('/api/v1/notifications', { headers: { Authorization: `Bearer ${token}` } }) }, [token]); return <section><div className="mb-8"><p className="mb-2 text-xs font-bold uppercase tracking-[0.2em] text-forest">Operations</p><h1 className="text-3xl font-bold tracking-tight">Notifications</h1><p className="mt-2 text-slate-500">Choose how operational alerts should be delivered.</p></div><div className="max-w-xl rounded-2xl border border-slate-200 bg-white p-6 shadow-panel space-y-4"><label className="flex items-center justify-between rounded-xl bg-mist p-4"><span><strong>Email alerts</strong><small className="mt-1 block text-sm text-slate-500">Requires SMTP configuration</small></span><input type="checkbox" checked={email} onChange={e => setEmail(e.target.checked)} /></label><label className="flex items-center justify-between rounded-xl bg-mist p-4"><span><strong>SMS alerts</strong><small className="mt-1 block text-sm text-slate-500">Provider integration required</small></span><input type="checkbox" checked={sms} onChange={e => setSms(e.target.checked)} /></label>{message && <p className="text-sm text-forest">{message}</p>}<button onClick={() => void save()} className="w-full rounded-xl bg-forest p-3 font-bold text-white">Save preferences</button></div></section> }
