'use client'
import { FormEvent, useState } from 'react'
export default function ResetPasswordPage() { const [password, setPassword] = useState(''); const [message, setMessage] = useState(''); const submit = async (e: FormEvent) => { e.preventDefault(); const token = new URLSearchParams(window.location.search).get('token'); const response = await fetch('/api/v1/auth/password-reset/confirm', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token, password }) }); setMessage(response.ok ? 'Password updated. You can sign in now.' : ((await response.json().catch(() => null))?.detail ?? 'Could not update password')) }; return <main className="grid min-h-screen place-items-center bg-forest p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-panel"><p className="text-xs font-bold uppercase tracking-[0.2em] text-forest">Fleet Operations</p><h1 className="mt-2 text-3xl font-bold">Choose a new password</h1><label className="mt-6 block text-sm font-semibold">New password<input className="mt-2 w-full rounded-xl border border-slate-200 p-3" type="password" minLength={8} required value={password} onChange={e => setPassword(e.target.value)} /></label>{message && <p className="mt-4 rounded-xl bg-mist p-3 text-sm text-forest">{message}</p>}<button className="mt-5 w-full rounded-xl bg-forest p-3 font-bold text-white">Update password</button></form></main> }
