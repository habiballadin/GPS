import { DomainPage } from '@/components/layout/DomainPage'
export default function SettingsPage() { return <DomainPage resourceType="settings" title="Settings" description="Organization, roles, integrations, notifications, and audit controls." /> }
